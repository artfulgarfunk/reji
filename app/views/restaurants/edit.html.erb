
<%= form_for @restaurant do |r| %>
  <%= r.label :name %>
  <%= r.text_area :name %><br />
  <%= r.label :description %>
  <%= r.text_area :description %><br />
  <%= r.submit "Update Restaurant" %>
<% end %>
